# --- API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Base de données ---
sqlalchemy==2.0.35
alembic==1.13.3
psycopg[binary]==3.2.3          # driver PostgreSQL

# --- Auth ---
python-jose[cryptography]==3.3.0  # JWT applicatifs
argon2-cffi==23.1.0               # hachage mot de passe (repli local)
httpx==0.27.2                     # appels OIDC (Entra ID)
authlib==1.3.2                    # client OpenID Connect

# --- Divers ---
python-multipart==0.0.12          # formulaires / uploads
email-validator==2.2.0
openpyxl==3.1.5                   # export Excel du référentiel (Lot 2)

# --- Lot 5 : matching, worker, stockage (optionnels selon fournisseur) ---
rapidfuzz==3.10.1                 # distances rapides (matching local)
celery==5.4.0                     # worker asynchrone d'import
redis==5.1.1                      # broker Celery + cache
# SharePoint (Lot 6) : Microsoft Graph via OAuth client credentials.
# httpx (déjà requis par l'auth Entra) suffit ; msal est optionnel.
# msal==1.31.0                    # gestion avancée des jetons Entra (optionnel)
# Connecteurs de stockage (installés selon le backend activé par l'admin) :
# azure-storage-blob==12.23.1     # backend Azure Blob
# boto3==1.35.36                  # backend S3 / MinIO

# --- Tests ---
pytest==8.3.3
pytest-asyncio==0.24.0
